namespace Agenda.Test
{
    public class Test
    {
        
        /* Para Multiplos resultados/respostas HTTP, usar o objeto __Results__:  
        // TESTE UNUTÁRIO
        app.MapGet("/", () => {

            var tarefa = new Tarefa(Guid.NewGuid(), "Verificar deploy MultiViewr em homologação", false);

            return Results.Ok(tarefa); // options: Ok(), BadRequest(), NotFound(), NoContent()...
        });
        */
    }
}